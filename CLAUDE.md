# 全域規範

> **本檔性質**:跨專案全域提示詞,主代理與所有子代理皆須遵循.與專案 `CLAUDE.md` 衝突時,以較具體的專案設定為準.

> **閱讀方式**:各節開頭的「觸發」標明何時適用;接到任務先掃各節觸發條件,命中即整節逐項比對後才動工.章節依任務生命週期排序:決策 → 調研 → 實作 → 規格 → 測試 → 文件.

> **本檔須能單獨成立,不依賴任何外部檔案**(會被帶到不同機器).故只留原則與可直接執行的規則:個案細節(哪個專案、哪天、賠了什麼)不寫進本檔,只保留去識別後的[典型錯誤形狀]——讓形狀本身可辨識,不必知道是哪個案子.

## 1. 語言與詞彙

- 一律用繁體中文回應.
- 詞彙等價:
  - [全域設定]＝[全域記憶]＝[長期設定]＝[長期記憶]＝[全專案設定]＝[全專案記憶]→ 本檔(`~/.claude/CLAUDE.md`).
  - [專案記憶]＝[專案設定]＝[計畫記憶]＝[計畫設定]＝[工作記憶]＝[工作設定]→ 當前工作路徑下的 `CLAUDE.md`(`<cwd>/CLAUDE.md`).

## 2. 決策與自主執行

**觸發**:所有任務,全程適用.

- 一律基於既有原則自行決定;唯有真的無法決定方向,或超出已知原則範圍,才問使用者.
- 一律不要詢問是否 commit;使用者需要時會自行說執行全部或部份 commit.
- 把任務轉化為可驗證目標:[加驗證]→寫無效輸入測試讓它通過;[修 bug]→寫重現測試讓它通過;[重構]→前後測試皆過.多步驟任務先列[步驟 → 驗證檢查點]計畫.夠強的成功條件才能獨立迭代.
- **可驗證目標的範圍依 §2.1 第 1 條取類別,不取投訴句**.**完成判準三件缺一不可**:①回歸(測試綠)②表覆蓋(§2.1 第 3 條之表每格為[已測/刻意允許/已知不修]之一,無空白)③**使用者視角驗收**——由我用真瀏覽器/真指令對表上每格實際操作一遍(不是只跑我自己寫的測試),發現全部寫進回報,含需求範圍外的.只有①而宣告完成＝未完成.
  - **無 UI 之交付物(函式庫/CLI/腳本/文件庫/技能庫)之等價物**:以**真實呼叫端的用法**跑一遍(真指令、真 import、真派工),並**驗產物是否落地**(檔案在不在、內容對不對、下游讀不讀得到),不是只看回傳值、離開碼或有沒有輸出.離開碼 0 與非空輸出都不是成功判準.沒有 UI 不免除本條,只換驗收介面.

### 2.1 全盤思考的做事原則(不是 spec 問題,是做事方式問題)

**觸發**:所有非 trivial 任務,含修 bug、順手改、被框成[小問題]的任務.
**觸發通則(適用本檔所有章節)**:各節的觸發一律以**對象**(凡涉及互動/刪除/路徑/派工/排版/測試…)解讀,**不以任務類型**(新增/重新設計/修 bug/順手改/重構)窄讀;讀既有碼命中對象即觸發.把[這只是順手改]當成免除條款,是本檔最常被繞過的一條.
**背景(LLM 天性,已多次重現)**:把需求縮成投訴句本身去修;矩陣的維度取自投訴用詞而非系統;自認確定就不對標常規;讀到既有碼就當設計繼承(連半吊子 affordance 都抄成註解);修了一種元素不回頭比對兄弟元素;測試綠即宣告[全盤完成].
**重複踩到的形狀**:同一個功能連續數輪被退,每輪都[讀了碼、實測、測試全綠],仍被一點就破——因為每輪都只沿投訴那一句話走一條線,沒有回頭列同類的其他線.**測試全綠只證明我測的那條線對,不證明類別完整**.這類問題不需要 spec 就能知道(同類產品通用常規+仔細列舉即可),**不得把原因推給[沒有 spec / 環境 / 工具]**.

1. **範疇取類別,不取句子**:投訴/需求落在某類別(互動、刪除、樣式、路徑、事件流…)時,交付物是**該類別在本系統的完整正確性**,不是那一句所指的點.接到[X 會閃],要交的是[X 所屬類別內所有同類行為皆正確];若只修那一點,必須明說[本次只修點,類別未查].
2. **先對標領域常規,再看本系統**:動手前寫下[同類成熟產品(至少兩個,如流程圖編輯器對應 React Flow / draw.io)對這件事的通用作法]與[本系統與之的每一處差異],差異逐條標[刻意/補丁/缺陷].**自認確定不免除此步**——判準不是確不確定,是有沒有寫下來.不確定者再上網查(§3).
3. **全列舉由系統推導,不由需求文字推導**:先列軸 → 每軸成員以 grep 全範圍自碼取得(§4 規則 1)→ 笛卡兒積成表 → 每格填[現況(file:line 或實測)/應然/處置].**表上不得有空白格**;空白格是工作缺陷,不是[沒問題].填完必做**缺軸自問**:對照下列標準軸逐一確認有無漏列.
   - 互動類標準軸:可命中元素(所有 `closest('.xxx')` 選擇器 + Teleport overlay)× 輸入事件(mousedown/move/up/click/dblclick/contextmenu/enter/leave/wheel/key)× 滑鼠鍵 × 進行中手勢 × overlay(popup/複選)× 能力旗標 × 終止路徑(mouseup/blur/destroy/旗標切換).
   - 資料/流程類標準軸:入口 × 前置狀態 × 並發/重入 × 失敗點 × 事件順序 × 清理路徑.
4. **對稱性檢查**:任何規則套到一種元素,必須對所有兄弟元素(如節點/邊/把手/齒輪/四角/轉折點/畫布/工具列)逐一比對;不對稱即缺陷,不是[另一邊本來就那樣].
5. **既有碼不是規格**:每個 guard/排除清單/`.stop`/affordance/註解,在我讀到時都要能用第 2、3 條的結論解釋;解釋不了的標為補丁,寫進表的處置欄,不得原樣繼承或抄成註解.**同一規則手寫 ≥2 處即補丁堆積訊號**,先收斂再修點.
   - **名詞錨(這些字在碼裡長什麼樣)**:**guard**＝開頭擋一下就 return 的判斷,如 `if (!enabled) return`;**排除清單**＝寫死的例外名單,如 `if (['node','edge'].includes(type)) return`、`if (e.target.closest('.handle-in')) return`;**`.stop`**＝Vue 的 `@mousedown.stop` 或原生 `e.stopPropagation()`,事件被截住,上層 handler 永遠收不到;**affordance**＝hover 放大/cursor/箭頭/把手這類[看起來可互動]的視覺承諾.非 UI 專案的等價物:早退檢查、白名單/黑名單、中介層攔截、預設值覆寫.四者的共同點是**都在讓某些情況不要走進來**——擋板本身不壞,壞的是講不出為什麼擋這個不擋那個.
   - **[解釋得了]的判準**:能講出**它擋掉的是表上哪一格,那一格的應然是什麼**.講不出來就是補丁,填進處置欄(收斂/移除/確認保留),**不准反過來寫成註解替它背書**——那是把缺陷升格成設計.典型錯誤形狀:某個把手有 hover 放大與 cursor,mousedown 卻在上層被丟棄,我讀到這個矛盾不但沒列為缺陷,還補了一句註解[此處不可作為出發點],於是下一個讀到的人直接繼承.
6. **半吊子即缺陷**:有 hover/cursor/箭頭等承諾卻無對應行為、有事件監聽卻被上層丟棄、有 prop 卻無效、有 A→B 就該有 B→A 卻沒有——不需被使用者觀察,發現即列缺陷並進表(§8 之[已被觀察]條件不適用於此類).
7. **產物才算全盤**:對標清單、笛卡兒表、對稱比對結果、使用者視角驗收紀錄,四者都是可交付的檔案;沒有這些產物,[全盤思考]只是形容詞.**落點與檔名固定**(免得每次自創格式):`./tmp/<案名>-對標.md`、`-矩陣.md`、`-對稱.md`、`-驗收.md`,或併成一份 `./tmp/<案名>-全盤.md` 以四個 `##` 章節分段;使用者指定路徑時從其指定.四份的最低內容——對標:同類產品至少兩個×每處差異×[刻意/補丁/缺陷];矩陣:軸清單＋每格[現況(file:line 或實測)/應然/處置];對稱:兄弟元素清單×同一規則逐一比對結果;驗收:每格實際操作一遍的觀察與落差.

## 3. 動手前與卡關時的思考紀律

**觸發**:接到新需求開始實作前;讀既有碼發現任何可疑之處(矛盾的 affordance、各自為政的 guard、對稱破缺)時;或問題反覆出現,效果一直不佳,陷入困境時.

- **上網調研**:技術細節不確定須先上網徹底進行網路之調研,絕不憑猜測盲試.
- **補丁堆積訊號**:讀到下列任一即視為補丁堆積,停止單點修,回 §2.1 第 3 條建表收斂後再修——①≥3 處各自為政的 guard、排除清單、`.stop`(名詞錨見 §2.1 第 5 條);②**對稱破缺**,即同型元素的規則不一致(如邊有排除、節點沒有;讀有防護、寫沒有);③同一條規則手寫在 ≥2 個地方.
- **失敗歸因先找自己**:被指出反覆失敗時,回答的第一段必須是我的流程缺陷(對照 §2.1 七條逐一檢核哪條沒做);環境因素(工具、沙箱、context 壓縮、缺文件)只能在其後列為次因,且須回答[即使該環境因素不存在,我是否仍會失敗?]——是,則環境不是原因.
- **替代方案(含反對使用者已指定的作法)**:須建立在具體證據上(見 §4 規則 4),先告知使用者討論,由使用者拍板再實作.
- **問題反覆存在或效果一直不佳**:先盤點問題內未明說的假設與潛規則,以此抽絲剝繭找核心;複雜問題須真正拆分,從中找出關鍵問題,而非一直圍繞已判斷/推測過但結果不佳的方向打轉(那是推卸責任,不是解決問題).
- **遭遇反覆無法解決的困境**:先檢視各問題間共通的固有信念與既定猜想,不讓這些假設引導思維或提前下判斷;各細節處皆是選擇,當前困境是一連串選擇累積的後果,回頭逐一檢視每個環節當初的選擇.
- **同類/同質問題或再引申/誘發諸多旁枝問題等反覆出現就須重構**:一直遇到持續或太多輪次之修補或出現無止境或無法收斂之問題,或一直無法觸碰與解決真正問題核心時,須先跳出思考與去判斷問題之核心,此時若須[重構]時就得建議使用者重構.重構時,函數該單一職責就要單一職責,該模組化就要模組化,該注入依賴就要注入依賴,函數或服務該無狀態就得無狀態,考慮用針對性重構或階段性分層改架構,才能真正把繁複與諸多連鎖問題進行完整修正.此外,重要與核心邏輯要獨立出來,才能建單元測試,要不然會因外層多處調用導致測試難以全面列舉,進而造成無法深挖出隱藏問題,或無法針對邊緣或邊界進行詳實與應對處理.
- **困難/複雜問題以中性角度分析核心**:探究多面向之正反/優劣/取捨與短長期利弊(警惕短期看似好但長期是壞的短視近利),全面探討後以階段性/逐步方式試探出新的判準與解法,逐步縮短與問題的距離,乃至完整解決.

### 3.1 卡住時的檢查清單(逐條寫下答案;寫不出來的那條就是卡點)

**觸發**:同一件事試了 ≥3 輪仍未解;修好一處又壞另一處;[看起來對了但使用者一試就破];或我開始出現[再試一次說不定就過]的念頭.
**用法**:**逐條寫下答案,不是逐條想過去**,落 `./tmp/<案名>-卡點.md`.答不出來、或只寫得出抽象句的那一條,就是真正的卡點,先解那一條再回頭.上面各條講的是心法,本清單是它們的可執行版本.

**一、先確認我卡在哪(先別急著再試一次)**

1. **這一輪跟上一輪的差別是什麼?** 具體寫出改了哪個參數、哪一行、哪句提示詞.若差別只是[再試一次]、[換個說法]、[再多加一個 if],那就是在打轉,停手直接跳到第 8 條.
2. **我修的是症狀還是成因?** 把因果鏈逐步寫出來:觀察到 X ← 因為 Y ← 因為 Z.**哪一步只是我推測、沒有 `file:line` 或實測支撐?** 那一步就是下一個要驗的東西,不是繼續往下猜.
3. **失敗是硬失敗,還是假成功?** 它是不是其實[離開碼 0、有輸出、畫面有東西]卻根本沒做事?依 §2 完成判準③驗**產物是否落地**,不看回傳值與有沒有回話.

**二、拆解與定位**

4. **我把哪三件事當成理所當然而沒驗過?** 逐條列出(如[這個函式一定會被呼叫]、[這個設定有生效]、[清單查得到就代表能用]).然後挑**最便宜驗證的那一個**先驗,不是挑最像兇手的那一個.
5. **系統邊界畫對了嗎?** 我 grep/讀碼的範圍,有沒有涵蓋被包裝的那一層(`node_modules/` 內的自有套件原始碼、所調用 CLI 自身、設定檔、環境變數)?**下[沒有 X]之結論前一律回 §4 規則 1 重畫範圍**.
6. **有沒有一個最小案例會成功?** 從失敗案例逐項拿掉變因直到它成功,再逐項加回——第一個讓它再度失敗的變因就是兇手.**做不出最小案例,代表我還沒真的把問題拆開**.
7. **能不能切成兩個可獨立驗證的小問題?** 寫出來,並標明哪一半我已經確定是對的(附證據).切不開就是還沒理解,回第 2 條重畫因果鏈.

**三、換路或停損**

8. **當初哪一個選擇造成現在的困境?** 逐一回看每個環節當初的選擇(選了哪個資料結構、哪條路徑、哪個工具、哪個預設值),逐條問[如果現在從零開始,我還會這樣選嗎?]——答[不會]的那一個就是該換的,不是繼續在它上面補.
9. **這是該重構,不是該再補嗎?** 對照上面[同類/同質問題…就須重構]之訊號;成立就停止修補,向使用者提出重構建議與範圍,不自行硬幹.
10. **停損**:同一條路走了 ≥3 輪仍無進展就**停止再試**,改為回報:已試過什麼(逐條)、目前確定的事實(附證據)、剩下兩三條可能的路與各自代價,由使用者拍板(見上面[替代方案]一條).**沒有新資訊就不要開第 4 輪**——那是拿使用者的時間換我的僥倖.

## 4. 回答「實際是什麼」問題的六條規則

**觸發**:使用者問[有沒有 X / X 是什麼 / Y 怎麼運作 / 按下去會怎樣 / 實際會怎樣]這類問題.

1. **grep 範圍夠廣才能下[沒有]結論**:須 grep 整個相關目錄全範圍(含相關 `node_modules/<套件>/`),不能只看一個檔就斷言.

2. **絕不寫[看起來像實作的虛構範例]**:只回實際存在的(`file:line` 為證);假設設計須先經使用者同意,明確標[非此系統實作],不寫成 code block,不與真實情況混雜.

3. **[實際 UI / Runtime 行為]優先實測,不讀碼推論**:按下去會怎樣,畫面長什麼樣,實際時序——跑 + 截圖比讀碼推論可靠,有 w-screenctl / playwright / curl 就用.
   - **與規則 6 的分流判別(先做這個再決定走哪條)**:[把相關檔案全讀完,答案是否唯一確定?]**是** → 屬靜態控制流,走規則 6 讀碼定案,不要再用間接測量;**否** → 屬 runtime 行為,走本條實測.兩條規則不衝突,分管不同類問題.

4. **負面斷言(不可行/太慢/撞 race)沒證據不要拋**:沒實際 benchmark/測試前不主動拋,也不因此 propose 繞道方案.判準對齊 §8 真痛三條件(在合約內+已被觀察+後果具體).§3 鼓勵的 propose 須建立在具體證據上.

5. **不確定時用[推測/看起來/可能]字眼**:無 `file:line` 引用又未實測 → 推測語氣並提示[需實測確認].

6. **靜態控制流問題:程式碼追到 file:line 就是答案,不用間接測量[再驗證]**:
   - 追鏈路要追到底,不能看表層 wiring 就推論關聯性.
   - 追到明確控制流(如 handler 第一行無條件呼叫某函數)即 100% 確定,不再用數 log / 看 metric / 跑副作用等間接測量驗證——間接測量本身有方法論風險,只會引入假矛盾.
   - 鏈路結論與間接實測衝突時,第一懷疑對象是實測方法(log 檔對嗎,有 cache/雜訊嗎),不是程式碼.
   - 分流判別見規則 3(讀完檔案答案唯一確定 → 走本條;否則走規則 3 實測).

## 5. 精準修改與簡潔優先

**觸發**:所有改 code 任務.

- **死碼處置**:注意到不相關死碼提出即可,不自行刪除;既有死碼除非被要求否則不刪;自己修改造成的孤兒 import/變數/函式則要移除.

## 6. 調研與方案規劃

**觸發**:非 trivial 需求的初期.初期盤查與方案列舉決定迭代深度(不足會讓對話從 3 輪拉到 10 輪).

**盤查層級**(L1-L2 通常已做,L3-L4 是關鍵且易被跳過):

- L1:組件 props,template 使用方式
- L2:組件往下的呼叫鏈
- L3:核心 util 的既有 `opt` 結構——既有 opt 是強訊號,表示函式本就接受 opt 擴充
- L4:副作用欄位如何被 UI 讀取——知道哪些[不想要但綁在一起]的副作用
- L5:類似場景的既有實作(grep 其他頁面)
- L6:**互動/狀態/事件流類:依 §2.1 第 3 條建輸入空間表**——方案列舉在建表之後,表沒填完不進方案(方案再好也補不了表的漏格).

**方案列舉至少 5 種,覆蓋 caller→callee 光譜**,每方案附改動檔案,預估行數,風險層級:

| 位置 | 代表做法 |
|---|---|
| Caller 預處理 | 資料進入前先過濾/轉換 |
| Caller 後處理 | 資料收到後剝除/調整 |
| Caller 替換組件 | 用低階組件重新組合 |
| Callee 加 prop | 共用組件加向後相容 prop |
| Callee 加 opt | 核心函式加向後相容 opt-in 旗標 |
| 核心改邏輯 | 直接修核心行為(最重) |

**要打破的三個不成文偏好**(否則[向後相容的核心 opt 擴充]這類最乾淨解法會被排除):①[共用/核心不要動]——opt-in 擴充比 caller 側 hack 乾淨;②[盤查停在表層]——核心 opt 慣例必看;③[列 2-3 個就夠]——非 trivial 應列 5+.

**業主/需求變動高時**:爆炸半徑最小化(改動鎖在業務頁面,單點優於跨檔連動);向後相容 opt-in > 修改既有行為;UI 一致性 > 程式簡潔.

## 7. 自有套件(wsemi / w-*)之開發與使用

**觸發**:專案以 JavaScript 為主,且涉及套件依賴偵測,安裝建議,升版,或修改 `wsemi` / `w-*` 套件時.

- 專案內 `wsemi` 與所有 `w-*` 套件皆為**自有套件**.
- 自有套件已自行規定並安裝其依賴(如 `wsemi` 內已含 `lodash-es` 等諸多套件);**一律不要提醒[未安裝直接依賴 / 建議安裝 X]**,不論是在套件內或專案層偵測到.
- 依賴出問題時,處置方式是針對該自有套件統一改版或升版,由開發者統籌;不在專案端個別補裝或繞道.

## 8. Audit / Review 報告的處置

**觸發**:接到審計/審查報告,或自己產出審查結論時.

審計給出[可能/潛在]風險時,先要求證據再考慮修改;拒絕為消除假想風險而引入確定退化.對[可能/潛在/推測]字眼的審計項加倍懷疑.

**本節的邊界**:三條件約束的是**審計報告中的推測性風險**;**不約束**我在任務類別內自己應該找出的具體缺陷(§2.1).使用者正常使用該功能會碰到的行為,一律視為合約內.不得拿本節當[不主動擴大範圍]的理由去略過類別內的缺陷.

### 真痛 vs 假痛三條件(ALL 滿足才算真痛,任一缺預設不修)

1. **在合約內**:對應使用者明確或隱含確認的需求邊界.把問題拉到合約外(多 agent 並發 / 跨 restart / 99.99% uptime / 自動恢復 / 主動健康監測)＝需求擴大型假痛,一律預設合約外,除非使用者明確確認.
2. **已被觀察**:實機重現過 / 出過 bug ticket / 維護中重複踩過.[未來如果/可能/萬一惡意 caller]是推測不是觀察.要證據(log,錯誤訊息,失敗紀錄);看上游約束(timeout/SLA 會不會反被[修復]破壞);看歷史(長期穩定運作＝強烈訊號不該動).
3. **後果具體**:能講出[不修就會踩到 X]的具體 X.[不夠乾淨/比較優雅/業界最佳實踐]不是後果——鍍金型與架構強迫症型假痛(判別:現在加功能,改功能,debug 痛不痛?不痛＝強迫症).

**第四類:契約缺陷(真痛,不需已被觀察)**:affordance 未兌現、同型元素互動不對稱、同一規則多處手寫、有 A→B 無 B→A——**定義、名詞錨與處置一律以 §2.1 第 5、6 條為準**(本節不重述),它們是使用者一操作即碰到的具體缺陷,列表後修,**不套三條件**.本節只補一條分野:**視覺/程式風格**上的不對稱是鍍金(適用三條件);**互動行為**上的不對稱是缺陷(不適用).

### 處理流程

先按三條件分類成 4 list:必修 bug / 功能疏失(待討論)/ 設計限制(文件化)/ 假痛(不修),分類先給使用者確認(LLM 對假想風險判斷力天然偏弱).確認後只修[必修 bug]——找最小改動,不翻轉並行/序列,同步/非同步等基礎模型;假痛寫入[已知不修]紀錄,不主動做.

## 9. 派工與模型調度

**觸發**:任務有規模(多檔探索,批量修改,獨立改 code 任務)而考慮委派時.

- 判斷任務難度,簡單任務降級執行:主代理無法切換自己的模型(`/model` 是使用者端指令),降級路徑是**派較便宜模型的子代理**(Fable→派 opus/sonnet;Opus→派 sonnet).
- 大量且獨立的改 code 委派子代理去改(或經使用者同意用 cli 派其他 AI 代理),主代理統籌追蹤,收尾審查,並於期間繼續其他工作;子代理改完逐項審查,確認正確才接受,有問題重發子代理修,不接受半成品.
- 派工門檻:2-3 個 tool call 內可完成的微型操作(改一行,rename,單檔小修)主代理直接做——派工 overhead(重新讀檔,理解任務,回傳驗收)比直接做更耗 token;有規模才派並依下表指定模型.
- 模型解析順序為 `model` 參數 > agent 定義 > 繼承主代理;不指定時繼承主代理模型(常為 Opus)→ 額度大量被燒,**派工須依工作性質顯式指定**:`Agent({ model: 'sonnet',...})` / workflow `agent(prompt,{ model: 'sonnet' })`.

| 子代理工作性質 | 建議模型 |
|---|---|
| grep 探索,找檔,列舉,彙整(Explore / 搜尋類) | **sonnet**(純機械可 haiku) |
| 單檔機械式改動,套既有 canonical pattern | **sonnet** |
| 深度推理的設計,審查,難 bug 定位,Plan 規劃 | **opus**(或繼承) |

fan-out 探索/機械改動類預設 sonnet,synthesis / 難判斷才 opus.

- 派工 prompt 必帶守則:子代理暫存檔一律落主 cwd 之 `./tmp/`(§12.4),探索禁 dump-to-disk;此守則須明文寫進派工 prompt.
- e2e/UI 測試耗時,由主代理統籌排程.

### 9.1 審計/複審/規劃審核類派工:先審表,再審方案(適用所有被派對象)

**觸發**:把設計複審、架構審查、規劃審核派給任何對象時——Claude 子代理、Codex、其他 CLI AI、工作流內之 agent 皆同;各 AI 之專屬技能只管「怎麼呼叫」,本節管「派什麼、怎麼驗收」.
**本節所稱[表]**一律指 §2.1 第 3 條之笛卡兒全列舉表(軸 × 軸,每格填[現況/應然/處置]),不是別的表.

- **被派對象必須能自行讀檔**;不能讀檔就不是獨立審查,先解決讀檔(見該 AI 之技能),不得改成把檔案內容貼進 prompt 充數.派長任務前先以「讀某檔並原文引用第 N 行」做最小探測;`validate` 須要求引用指定內容,不只驗非空.
- 複審的價值在**找出我漏掉的格**,不在評價我已列的方案;方案再好補不了輸入空間的漏格.prompt 必依序要求:①挑戰 §2.1 第 3 條之表的完整性(缺軸、缺格、格內現況不屬實者,附 `file:line`)②審對標清單與對稱性 ③最後才審方案與步驟.
- 表尚未建好時先建表,不派工;沒有表就派審方案＝把漏格帶進實作.
- 收回後對其每一格逐一核對源碼(`file:line`)再採納,不整批接受,也不整批駁回;採納/駁回逐條附理由.
- **自審的表一定有洞**:曾有一張我自認完整的互動矩陣,交外部複審後仍被找出 **30 格缺口且全部屬實**.所以派工不是[請你確認我對],是[請你找我漏的格];不能因為自己檢查過就跳過複審.

## 10. 實作規範:UI / UX

**觸發**:任何涉及前端畫面/元件/互動的改動.

### 10.1 基本原則

- 新增功能時絕不修改既有 UI,版面,陰影,視覺效果.
- 視覺效果(過場,陰影,hover)在相關元素類型間須一致,主動驗證不等使用者指出.
- 非使用者同意不可修改既有範例的拼寫.

### 10.2 「重新設計」是整體重做,不是微調既有 prop

**觸發詞**(出現任一,一律當[整體重做]處理,不可降級成[調樣式]):「重新設計」「重做」「redesign」「要有設計過 / 設計感」,以及任何對美感 / 版面 / 比例 / 對齊 / 留白 / 風格的不滿.

**命中觸發詞之任務,必先調用 skill[role-design-web-for-redesign](交付前 16 條必做+RWD 五鐵則+間距對稱原則),整篇入 context 逐項比對後才動工.**

### 10.3 禁用瀏覽器內建 `alert()` / `confirm()` / `prompt()`

production code 一律改用專案既有 DOM 模態元件(grep `WAlert` / `CheckYes` / [System message]用法沿用);僅臨時 debug 可短暫用,不得留進 commit.why:原生 dialog 是 OS 層視窗不在 DOM——Playwright 截不到圖,要掛 `page.on('dialog')`,block JS 執行緒,UX 不一致不可樣式化;DOM 模態則 locator + 文字斷言 + pixel baseline 全可測.看到既有 `alert()` 應提報並經同意後改 DOM 模態.

### 10.4 Vue UI 程式規範(async 提交 / `@event` handler / `promiseUnlock`)

**凡撰寫或修改 Vue UI 之非同步提交流程(loading/錯誤處理),template `@event` handler,或 `promiseUnlock` 按鈕的任務,必先調用 skill[role-coder-for-vue-ui],整篇入 context 逐項比對後才動工.**

### 10.5 展示形式須匹配欄位型別/語義,交付前逐欄自掃,不等使用者抓

**觸發**:建置或修改任何[展示項目資料]頁面/彈窗(read-only viewer 類).

**問題根源(LLM 天性)**:建 viewer 時傾向把所有欄位機械複用同一種[label+文本框]組件,純數值(如座標 200)孤懸於全寬多行框內,一看就是未經設計.此類錯配使用者一眼可見,LLM 卻不會自覺——因為[每欄都套同組件]在程式上是最省事路徑.

**強制流程**:

1. **動工前先讀 schema 按型別分流**:專案 schema 通常帶有欄位型別與單位資訊(欄位名各專案不同,先在該專案的 schema 找出[型別]那一欄),展示形式依型別選擇:
   - 純數值 → 緊湊統計格(label 小字+等寬大號數值+單位小字,一組相關數值並排)或單行呈現,**絕不用全寬多行文本框**
   - 百分比/進度語義 → 進度條
   - 枚舉/分類/關鍵字 → chip/badge
   - 多行長文本 → 文本區塊(這是文本框唯一正當場景)
   - URL → 可點連結;日期 → 格式化短文本
2. **檢核反射**:寫每個展示欄位時自問[這欄的值平均多長,什麼型別?]——值短(數字/代碼/枚舉)卻配全寬多行框＝錯配,當場改.
3. **交付前逐欄自掃一輪**:對整頁每個欄位跑一次檢核反射,並確認同型別欄位跨系統/跨頁面呈現一致(同專案已有 canonical 數值展示組件就沿用,沒有就建一個供全專案復用).
4. 此原則不限數值:任何[呈現形式與資料語義不匹配]皆屬同類問題(如布林用長文本框,顏色值用純文字不帶色塊預覽),一併自掃.

### 10.6 觸碰既有排版或新增小元件:量數字+盤色票,工程任務也適用

**觸發**:凡動到既有元件之排列(搬移/分拆/接排按鈕群,調整定位)或新增任何小型 UI 元件(下拉/輸入框/chip/badge/膠囊)——**即使任務被框成「功能搬移/重構」而非「設計」**;第 4 條另於**讀到或改到任何既有互動指示符號/affordance 時**觸發(不限新增).
**背景(LLM 天性)**:把 §10.2 量測義務窄化成只有[重新設計]觸發詞才啟動(觸發通則見 §2.1:以對象解讀,不以任務類型窄讀);順手改排版/新增小元件被歸成工程任務→整套[讀API→量數字→盤設計語言]被跳過;配色走[隨手給個看似合理的色]之最省事路徑.
**典型錯誤形狀**:按鈕群拆搬後接排間隙量到 17px、原生只有 5px,未量測即交付;新下拉未盤點父層深色玻璃色票就隨手配色.

1. **排版數字前後對照**:動工前量原始排版數字(間距/對齊/尺寸,`getBoundingClientRect` 取真值),改後複量同組數字,一致才可宣告完成.只驗[功能會動][有排上去]＝未完成.
2. **配色先盤點後沿用**:動工前盤點[所在組件+鄰近組件]之色票並明文列出——主題(焦點/選中)色,hover 色,底色層次,文字階層,邊線色;新元件一律沿用,禁止憑空配色(隨手挑深色/淡色也算憑空).無可參考時才允許新色且須先報備.
3. **原生 `<select>` 禁入自訂主題區**:其彈出清單之選中/highlight 色由 OS/瀏覽器決定(如 Chromium 淡藍),CSS 不可控;頁面有自訂主題(深色玻璃等)一律改用專案自製 popup 選單(如 WPopup)重現主題,原生 select 僅限無主題要求之內部頁.此為 §10.3 禁用原生 dialog 之同族規則.
4. **互動指示符號是行為承諾,兌現不了就移除(新增與既有皆適用)**:箭頭/chevron/把手/開關/hover 放大/cursor 等符號暗示[可互動+有狀態];放了就必須完整兌現;**讀到既有碼有承諾無行為者,一律依 §2.1 第 5、6 條列為缺陷,不得抄成[設計如此]之註解**.兌現的標準——尺寸可辨識,且狀態聯動(如下拉開啟時箭頭旋轉或高亮).狀態接不出來或不打算接時一律不放,由容器自身之 hover 回饋指示可點即可;半吊子指示比沒有更糟——靜止不動的▾會被使用者當成壞掉/功能瑕疵.**典型錯誤形狀**:下拉配一個 0.6rem、永遠朝下、不隨開闔變化的 ▾.
5. **固定尺寸給值須計算,不可拍腦袋**:給固定 width/height 前,先量內容最長項實寬(隱藏span量測或 getBoundingClientRect)+組件內部結構(padding/邊框/icon/間隙)逐項加總,再留小而對稱之呼吸空間(數px級);猜一個[看起來夠]的值→過寬右側大片留白,過窄截字,皆為未經設計.**典型錯誤形狀**:最長內容(如 `'2:2'`)實寬僅 16px、結構加總自然寬約 50px,卻拍腦袋給 74px → 右側留白突兀.
6. **下拉/選單之高亮不可破壞文字對齊參考線**:選項文字左緣須與 trigger 內文字左緣貼齊(padding 對齊,量測驗證);選中高亮用[不佔位.不干擾對齊]的方式(整列填色/文字變色),左緣bar.前置icon.縮排等會在文字左側添加視覺元素之高亮,於窄選單會讓文字看起來右偏.對齊參考線變亂,禁止照搬自寬列表之高亮語言.**典型錯誤形狀**:54px 的窄選單照搬寬列表的左緣 bar 高亮,選項 padding(13px)又與 trigger(9px)不一致,雙重破壞對齊.

## 11. 實作規範:路徑與檔案 I/O

**觸發**:任何組裝路徑,寫檔,跨 shell/程式傳遞路徑的操作.

### 11.1 兩種出檔場景

- **場景 A:輸出落在使用者工作路徑**(測試暫存,報告,腳本產物)→ 用 cwd-relative(`./tmp/foo.json`).絕不用 `fileURLToPath(import.meta.url)`——套件被 install 後輸出會落到 `node_modules/<pkg>/`,搬動腳本時輸出位置悄悄改變.
- **場景 B:輸出落在腳本/模組所在資料夾**(套件自帶 user_data)→ 用 `fileURLToPath(import.meta.url)` + `dirname` + `join`.

### 11.2 永遠不要用 `new URL(import.meta.url).pathname` 取路徑

`pathname` 回 URL-encoded 字串:空格變 `%20`,中文變 `%E9%96%8B...`,Windows 多 leading `/D:/`,fs 會把這些字面字元當實體資料夾名建立(踩過:D 槽根目錄建出多層 `%20%E9...` 垃圾資料夾).URL → 實體路徑只有 `fileURLToPath()` 一條路,且僅場景 B 用.

### 11.3 git-bash 的 POSIX 路徑(`/d/...`,`/c/...`)絕不可跨界傳給 Windows 原生程式或 node

[`/d/` = D 槽]映射只存在 git-bash shell 內部.把這種字串交給 Windows 原生 exe 或 node 的 path/fs,開頭 `/` 被當[當前磁碟根]:`/d/tmp/x` → `D:\d\tmp\x`(多一層 d),`/c/Users/x` → `D:\c\Users\x`(磁碟機都錯).邊界判別:路徑字串最後是誰解析——Bash 工具內 `mkdir /d/tmp/...` 對;寫進 `.mjs` 再交給 spawn / `launchPersistentContext` / node fs 就錯.改用帶磁碟機代號路徑(`D:/tmp/x`)或從 `fileURLToPath` 衍生.

### 11.4 寫入前驗收

路徑經組裝(join,replace,URL 轉換)後,fs 操作前先 `console.log(path.resolve(...))` 印 absolute 路徑驗收;寫入後 `ls` 確認落點;發現孤兒資料夾立刻徵求允許後刪除.警覺訊號:工作目錄含空格,中文/非 ASCII,Windows `D:\...`——任一符合就雙倍仔細.

### 11.5 HTTP 靜態檔案服務:request 路徑是 URL-encoded,解碼與穿越防護必成對

**觸發**:以 Hapi/Express 等自建靜態檔案服務(SPA dist 之 `{any*}` 路由),或以 request 路徑組裝檔案系統路徑時.

- `request.path`/`url.pathname` **保留 percent-encoding**(§11.2 同族:URL 空間字串≠檔案路徑).直接 `path.join` 去找檔案,非 ASCII 檔名(中文圖檔)必找不到而**靜默落入 SPA fallback → 回 200+text/html → 瀏覽器破圖**;ASCII 資產(css/js)恰好正常,極易誤判為[圖檔沒上傳].
- 修正三要件缺一不可:①`decodeURIComponent` 包 try/catch(非法 % 序列拋 URIError);②**解碼後必做路徑穿越防護**——未解碼時 `%2e%2e` 恰好擋住 `..`,加了解碼等於同時打開穿越風險,兩者必須成對;③落點檢查用 `path.resolve` 後絕對路徑且尾端補 `path.sep` 再 `startsWith`(只比 `startsWith(fdAbs)` 會放行同前綴兄弟目錄).
- 驗收不可只看 200:**fallback 也回 200**,須對[非 ASCII 檔名請求]斷言 Content-Type(如 image/png 非 text/html);另測穿越與非法 percent(`/%` 不得 500).框架層另有各自的落點限制(如 Hapi/Inert 之 `confine` 會對 `relativeTo` 外之檔案回 403 且無說明,Windows 磁碟機代號大小寫不同即視為不同路徑),服務目錄一律用 cwd 相對路徑交 `path.resolve` 展開,勿寫死絕對路徑.

## 12. 實作規範:Shell,工具與環境

**觸發**:寫腳本,跑指令,管理程序/服務,傳檔.

### 12.1 工具選用

- 腳本優先 `.mjs`(ES modules),優先用專案內 `node_modules` 套件;非必要不寫 `.js`,`.py` 次之.
- 盡量避免 PowerShell;不得已要用先徵詢使用者.
- **`.bat` 含中文一律 Big5(cp950)編碼**:cmd.exe 用系統 codepage 解析,UTF-8 各種 workaround(BOM,`chcp 65001`)實測都壞.不要默默把現有 Big5 .bat 重存成 UTF-8.

### 12.2 Bash / PowerShell 工具內禁用之寫法:`cd` 與 `cat` 系檔案 I/O

**觸發**:任何要在非 cwd 之目錄下執行的操作(解壓,安裝,建置,跑他處腳本,查看子目錄產物),以及任何建立/讀取/修改檔案之操作(寫暫存腳本,產設定檔,看檔案內容).

**共同 why**:此二類寫法會**觸發權限確認彈窗**,連續打斷使用者;專用工具(Read/Write/Edit/Glob/Grep)則無此問題,且有檔案狀態追蹤,行號顯示與 diff 呈現.

#### (1) 不用 `cd`,改用絕對路徑或指令自身之目錄參數

**鐵則**:工具的工作目錄本來就是主 cwd 且**跨呼叫持續存在**,不需要也不可以用 `cd` 去切.要在別的目錄動作,用絕對路徑,或用指令自己的目錄參數:`tar -C <dir>`,`npm --prefix <dir>`,`npm pack --pack-destination <dir>`,`git -C <dir>`,`node <dir>/x.mjs`,`ls <dir>`.

**why**:除觸發確認外,`cd` 會改掉後續每一次呼叫的 cwd,原本正確的相對路徑(`./tmp/x`,`db01_.../y.csv`)全部指到錯的地方,為了修正又得再 `cd` 回去,於是**每一條指令都要使用者按同意**,形成連鎖打斷.**典型錯誤形狀**:為了解壓一個套件用了 `cd ./tmp/xxx`,其後數條指令全部跳出確認,使用者反問[為何現在這指令都會跳出來要我同意].

**已經誤 `cd` 出去時**:不要再用 `cd` 切回來(那是第二次觸發),直接改用絕對路徑執行後續指令即可;需要確認目前位置用 `pwd`.

#### (2) 不用 `cat` / heredoc / 重導向做檔案 I/O,改用 Read / Write / Edit 工具

| 意圖 | 禁用 | 改用 |
|---|---|---|
| 建立檔案(含 `./tmp/` 暫存腳本) | `cat > f << 'EOF'`,`echo ... > f`,`printf ... > f` | **Write 工具** |
| 讀取檔案內容 | `cat f`,`head -n f`,`tail -n f`,`sed -n 'a,bp' f` | **Read 工具**(可帶 offset/limit) |
| 修改檔案局部 | `sed -i`,`awk` 覆寫 | **Edit 工具** |
| 找檔名 / 搜內容 | `find`,`ls -R`,`grep -r` | **Glob / Grep 工具**(見 §12.3) |

**why**:除觸發確認外,②shell 重導向寫檔多一層引號與跳脫(中文,`$`,反引號,`!`)全得自己顧,heredoc 還受 `<<'EOF'` 與 `<<EOF` 之展開差異影響,錯了是**靜默寫出壞內容**;③Write/Edit 之改動會呈現 diff 供使用者審閱,shell 寫檔則完全不透明.**典型錯誤形狀**:以 `cat > ./tmp/xxx.mjs << 'EOF'` 寫一個暫存腳本又跳確認,使用者反問[為何又出現會要我確認指令].

**例外**:確實需要接管線做流式處理(如 `node x.mjs | grep -c pattern`,`wc -l < f`)才用 shell;**產出檔案一律 Write**,不因[只是暫存腳本]而放寬.

### 12.3 搜檔案內容一律用 Grep 工具,禁用 Bash `grep -r`

**觸發**:任何在專案內找字串,找引用,確認[有沒有 X]之操作(§4 規則 1 之 grep 全範圍盤查亦適用).

**鐵則**:搜內容用 **Grep 工具**(內建 ripgrep),找檔名用 **Glob 工具**;**不要用 Bash 的 `grep -rn` 與 `find`**.

**why**:資料型專案之 `_plot`,`_report`,`_selfStorage`,`_logs`,`_holes` 等目錄動輒數萬至數十萬檔案.Grep 工具之 ripgrep **預設尊重 `.gitignore`**(這些資料目錄通常已被 ignore),且多執行緒遍歷並自動跳過二進位檔,實測秒回;Bash `grep -r` 完全不知 `.gitignore` 存在,一律硬掃全樹,實測直接吃滿 120s timeout 被丟到背景.

**最易踩之誤區**:以為加了 `--include="*.mjs"` 就安全 —— **不是**.`--include` 只決定[要不要讀這個檔的內容],grep 仍須走遍每個目錄並對每個檔案 stat.**瓶頸是檔案數量而非內容大小**,故 `--include` 完全救不了.**典型錯誤形狀**:同一輪對話內因此連續吃滿 timeout 兩次,兩次都以為[加了 `--include` 就沒事].

**真的須在 Bash 內搜**(如要接管線做統計)才用,且二選一:

```bash
git grep -n "pattern"                      # 只搜 git tracked 檔案, 過濾範圍天然與 .gitignore 一致(首選)
grep -rn "pattern" --include="*.mjs" \
     --exclude-dir={node_modules,_plot,_report,_selfStorage,_logs,tmp} .
```

**同理**:`find . -name`,`ls -R`,`tree` 於同類專案亦會逾時,改用 Glob 工具;子代理探索更禁 `tree > 檔案` 之 dump-to-disk(§12.4).

### 12.4 暫存檔一律 `./tmp/`

**臨時/暫存/scratch/探索 dump 檔一律落在主任務 cwd 下的 `./tmp/`**(錨點＝主 agent 啟動時的 `process.cwd()`;子代理在他處須用主 cwd 絕對路徑指向同一 `./tmp/`).不可用 `/tmp`,`$TMPDIR`,`$HOME`(Git Bash 的 `/tmp` 映射到使用者 Temp,檔案跑出專案外);cwd 根目錄雜檔(`./tmp_tree.txt`,`./debug.log`)與 `./.cache/` 也不行,集中單一 `./tmp/` 才能 `rm -rf tmp/` 一鍵結案.子代理探索禁用 `tree > 檔案` dump-to-disk,改 Glob/Grep/讀 stdout.**派工子代理時 prompt 須明文加上此守則**(§9).任務結束前驗證 `./tmp/` 已清乾淨(須保留則說明).

### 12.5 Windows / Git Bash 下 `taskkill` 與取 PID

- `taskkill /F /PID <pid>` 在 Git Bash 會失敗(bash 把 `/F` 當路徑).正確:`cmd //c "taskkill /F /PID <pid> /T"`(雙斜線避免 bash path 翻譯).取 PID:`netstat -ano | grep ":<port> " | grep LISTENING | awk '{print $5}' | head -1`.
- **取 PID 勿依賴 wmic**:Windows 11 已移除 wmic,靠 wmic 撈 PID 會回空輸出且不報錯(看似成功實未殺,程序續佔資源).改用 tasklist(需比對 CommandLine 時用 PowerShell `Get-CimInstance Win32_Process`)取 PID 逐一 `taskkill /F /T`,殺後必回驗(見 §12.6).
- 何時可主動 taskkill:專案 CLAUDE.md 明確授權或使用者明確要求,否則先回報.

### 12.6 刪除 / 殺程序 / 重啟後必「回驗」成功(勿假設)

kill 與 rm 皆**不保證成功**,執行後必回頭驗證,不可假設已成功:

- **殺程序**:taskkill 後再 `netstat`/`tasklist` 確認該 PID **已消失,埠已釋放**.用埠號抓 PID 只殺得到「當下在監聽」者——卡住/未監聽之舊實例會漏殺.
- **刪檔/資料夾**:`rm -rf` 後以 `[ -d 夾 ]`/`ls` 確認**真的沒了**(rm 可能因鎖定/權限靜默失敗).Windows 下對 memory-mapped 檔(如 LMDB `.mdb`)之 rm 會移除目錄名,但**行程仍持有舊檔映射**,存活之舊程序會無聲**續用舊資料**(假象「刪了,重建了」).
- **重啟服務**:健康檢查(如 `curl → 200`/ping)**無法分辨新舊實例**——存活之舊實例照樣回應,據此判定「重啟成功」是假陽性.通用判準是**確認新行程確實是當前提供服務者**:`netstat -ano`/`lsof -i` 查監聽該埠之 PID,須等於**你本次啟動的行程 PID**(或至少舊 PID 已消失,且出現一個新監聽 PID);並檢查新行程之輸出/log **無綁定失敗**(埠被占用類錯誤,各語言字樣不一,如 `EADDRINUSE`/`address already in use`/`bind: 已使用`).否則新實例常因埠被舊實例占用而**啟動即失敗退出**,舊實例續服務舊狀態(現象:「重啟了但狀態沒變/吃到舊快取」).服務專屬的「已啟動」log 字樣屬各專案細節,記於該專案設定而非此處.

### 12.7 `gcloud compute scp` 傳檔

**觸發**:以 gcloud 對遠端執行個體傳檔時.遠端目的一律用絕對路徑 `/home/<user>/...`,**絕不用 `~`**(底層 pscp 不展開 `~`,會 `unable to open`;但 `ssh --command` 內的 `~` 正常展開,別搞混).中文來源路徑、Unicode 檔名、磁碟機代號實測全支援,不需 ASCII 中轉.二進位一律走 scp,不要用 `cat | ssh "cat > file"`(Windows 下 stdin 串流不可靠).

### 12.8 長任務:`run_in_background` 不受 timeout 限制,長任務一律掛監聽不輪詢

**觸發**:任何預估超過 2 分鐘之作業(測試全跑,批次重產,派工外部 CLI AI,下載/爬取,建置).

**凡要「現在停下,之後被叫醒再繼續」——定時任務,延後執行,等背景指令/外部程序/排程輪次完成,等檔案或日誌變化,或用到 `Monitor`/`sleep`/`CronCreate`/`ScheduleWakeup` 任一者,不論使用者要求或 agent 自發——必先調用 skill[role-setup-scheduler-for-session],其第一步是自檢是否位於 VS Code 外掛:外掛內 `CronCreate`/`ScheduleWakeup` 永遠不觸發,只有背景行程(`run_in_background` 之 `sleep`/`until` 輪詢)與 `Monitor` 會叫醒;等待須有上限出口,醒來要做的事先落檔.**

#### 鐵則一:`timeout` 只約束前景,`run_in_background: true` 不受其限,亦無 10 分鐘上限

**2026-08-15 實測**(Windows/Git Bash):

| 測試 | `timeout` 參數 | 實際耗時 | 結果 |
|---|---|---|---|
| A | `60000`(60 秒) | 120 秒 | 完整跑完 exit 0, 正常送通知 |
| B | `600000`(10 分鐘) | 11 分 04 秒 | 完整跑完 exit 0, 正常送通知 |

即 `timeout` 之上限(600000)僅為**前景阻塞**之上限;帶 `run_in_background: true` 時該值不生效,任務跑多久都不會被截斷.

**故:長任務不需要 `nohup`/`&`/`disown`,不需要自寫輪詢迴圈**.直接:

```
Bash({ command: 'node ./tmp/dispatch.mjs', run_in_background: true })
```

harness 全程追蹤該行程,**結束時主動送出 `<task-notification>`**(含 `output-file` 絕對路徑,內含 stdout+stderr);期間隨時可 `Read` 該檔看中途進度;要中止用 `TaskStop`(帶 task id),不必再撈 PID.

#### 鐵則二:要[逐事件通知]用 `Monitor`,要[單次完成通知]用 `run_in_background`

| 需求 | 工具 | 寫法 |
|---|---|---|
| 只要一次[做完了] | Bash `run_in_background` | 直接跑該任務;若任務已在他處跑,則跑 `until ! ps -p $(cat tmp/run.pid) >/dev/null 2>&1;do sleep 2;done` |
| 每次發生都要通知(進度,錯誤,CI 各步驟) | `Monitor` | `tail -f tmp/run.log \| grep -E --line-buffered "<成功\|失敗訊號>"` |
| 整個 session 都要盯(常駐服務 log) | `Monitor` + `persistent: true` | 無 timeout,以 `TaskStop` 收 |

`Monitor` 之 `timeout_ms` 上限 3600000(1 小時),`persistent: true` 則為 session 長度.**stdout 每行 = 一則通知**,故 filter 要選擇性;但——

**`Monitor` 覆蓋率鐵則:沉默不等於成功**.filter 只抓成功字樣時,崩潰/卡死/非預期退出全都靜默,而[靜默]與[還在跑]長得一模一樣.掛之前自問[此刻若它崩了,我的 filter 會吐出東西嗎?]不會就放寬:

```bash
# 誤: 崩潰時完全靜默
tail -f tmp/run.log | grep --line-buffered "done"
# 正: 成功與失敗訊號一併涵蓋
tail -f tmp/run.log | grep -E --line-buffered "done|Traceback|Error|FAILED|Killed|OOM|exit code [1-9]"
```

管線每一段都要逐行 flush:`grep` 要 `--line-buffered`,`awk` 要 `fflush()`;`head` 無法 flush,`| head -N` 會整段吞掉.stderr 不進事件流,自己跑的指令要 `2>&1` 併入.

#### 鐵則三:仍須 detached(`nohup` / `Start-Process`)的唯一理由是[跨 session 存活]

`run_in_background` 之行程**掛在 Claude Code session 之下**;session 更替/重啟(API 529,連線中斷,session 逾時)時會被一併終止(**已觀察**:一次全量重產在 session UUID 更替時被一併終止,992 項只完成 239 項).

故判準是[會不會跨 session],不是[跑多久]:

| 情境 | 用法 |
|---|---|
| < 2 分鐘 | 前景 Bash |
| 任意時長,可接受隨 session 結束 | `run_in_background: true`(**不設** nohup,**不設**時長上限) |
| 數小時級,或不可因 session 更替而中斷 | detached(`nohup ...& disown`;Windows 用 `Start-Process`)**再另掛 `Monitor` 盯 log** |

detached 之情形下,監聽仍走 `Monitor`(逐事件)或 `run_in_background` 包 `until ! ps -p ...` 迴圈(單次完成),**不要人工輪詢,不要留無通知之空窗期**.

**中斷 detached 任務(關鍵陷阱)**:`$!` 是 **MSYS2 PID**——`ps -p` 認,`taskkill` 不認(永遠回找不到).必須先用 wmic 撈 Windows PID 再殺:

```bash
WPID=$(cmd //c "wmic process where \"name='node.exe'\" get commandline,processid" 2>/dev/null | grep -i '<script-name>' | grep -oE '[0-9]+ *$' | tr -d ' \r')
[ -n "$WPID" ] && cmd //c "taskkill /F /T /PID $WPID"
```

(Windows 11 無 wmic,改用 PowerShell `Get-CimInstance Win32_Process` 以 CommandLine 比對取 PID,見 §12.5.)Linux/macOS 直接 `kill -9 $(cat tmp/run.pid)`.自動中斷流程須先徵詢使用者(detached process 可能正寫 DB / 產 baseline),除非明確授權.

### 12.9 Windows 背景 Chrome(playwright/puppeteer)程序回收

**觸發**:任何專案以 node 調用 chrome/chromium 背景繪圖或截圖(playwright、puppeteer、w-html2png 系),涉及常駐服務、排程重啟、或需清理 chrome 殘留時.

**成因**:Windows 上 playwright 以 non-detached spawn 瀏覽器且不用 Job Object,清理只掛在 node 的 exit/signal handler——node 被 **TerminateProcess**(`taskkill /F`、崩潰、OOM)時 handler 全不執行,chrome 整樹孤兒化,服務自動重啟則逐次疊加.`--remote-debugging-pipe` 斷線自關是**盡力而為非保證**(重載/卡頓機器實測不會自關),不能當防線;上游無解也不會修,須自建回收.

**回收三層**:①**啟動自掃**——常駐服務埠綁定成功後(證明無其他實例服役),查 chrome.exe 之 ExecutablePath(**限定自家瀏覽器路徑**,避免誤殺使用者瀏覽器)且**父行程已死**者,樹狀殺;②**看門狗掃已停止(stopped)之 chrome**——卡死者用一般枚舉或年齡判準看不出來,須以 `w-process-status` 之 `WProcessStatus('chrome.exe')` 查 `status==='stopped'` 才找得到,交叉自家路徑後樹狀殺;③**關閉走 tree-kill**——用 `wsemi` 之 `execProcessKillPid`(Windows 內部即 `taskkill /pid X /T /F`),**不可用 `child.kill()`**(Windows 下等同 TerminateProcess 且只殺 node 本身).

**鐵則**:①殺 chrome 必殺 root 帶 `/T`,逐個殺 renderer 無效(孤兒主程序會立即補生);②判準用[歸屬(父死)+健康(stopped)],**絕不用年齡**——常駐瀏覽器持續有請求時合法活數天,年齡判準會週期性處決健康現任;③`HandleCount=0` 之終止中卡死殭屍任何工具都殺不掉(回報[查無執行中實例]),屬 OS 層狀態,殺掉 root 後不增生即可接受,重開機歸零.

## 13. 版本控制(Git)

**觸發**:使用者要求 commit 時(不主動問是否 commit,見 §2).

**[全部commit]一律單一 commit,不主動依議題拆分**:`git add` 全部後單一 commit,message 用多段 bullet 涵蓋所有議題.例外:使用者明確說[分開/拆/按議題]才分.why:使用者要的是[動詞 commit + 受詞全部],自作主張拆分＝擅自加工指令.

## 14. AI 互動安全

本項針對 Claude Code / Anthropic API.

**觸發**:要用 `Read` 讀取任何[經 HTTP/IPC 抓回的圖檔]之前.

### 把外部來源[圖檔]送進 API 前先驗 magic number

`Read` 把圖片以 base64 image block 送 API;內容非合法圖片 → 400 → 壞 block 綁進 history → 每個後續請求永久 400 → **session 變磚不可恢復**.高風險:任何經 HTTP/IPC 抓回的[圖檔]——w-screenctl `/chrome/screenshot` 回 JSON wrapper(`{ok,image:<base64>}`)不是 raw PNG;API error body HTTP 200;redirect/auth 頁被存成 .png.

強制流程:①`Read` 圖檔前先 `file <path>` 驗 magic number(PNG `89 50 4E 47`);②已知 JSON wrapper 先解:`curl -s .../screenshot | jq -r .image | base64 -d > ./tmp/shot.png && file ./tmp/shot.png`;③不假設[`-o file.png` 就是 PNG].

撞到 400 後:該 session 不可恢復,立刻新開 session 口頭交接進度,並刪除/改名壞檔避免再讀.

## 15. 規格 Spec

**觸發**:從 spec 衍生 predicate/實作/文件;spec 有歧義或缺漏;實作、實測、產圖或撰寫手冊時發現與 spec 不符;新寫、改寫、擴充、複審 spec.

### 15.1 名詞錨:什麼算 spec

- **算**:流程文件(含 e2e 流程 spec 之 `E2E-NNN` 案例與其〈畫面元素對照〉〈訊息對照〉〈規則摘要〉〈已知落差〉)、i18n 鍵與訊息原文、schema、API 規格、專案共用文件(設計要點與取捨、ADR、裁決紀錄)、先前已定案之結論.
- **不算**:舊版說明文件、上一版草稿、程式碼註解、我對程式碼或畫面的推論.這些只能當線索;要進 spec,須先以程式碼(`file:line`)或實機(探測紀錄)證成.
- **草稿每一句都是假設**:接手既有草稿時逐句標[已實測/未實測/實作猜測],未實測者重探測,實作猜測(如[須遮罩][參數為 X])刪除,不因它已經寫在那裡就繼承——這是 §2.1 第 5 條[既有碼不是規格]在文件層的同一條.

### 15.2 讀 spec 衍生 predicate,使用者不是 spec 的二次審核員

LLM 的職責是讀 spec 衍生 predicate/實作/文件,不是把整份理解丟回給使用者 re-review([我列預期清單你 review 一下]＝把讀 spec 外包給使用者).

**因果方向**:✅[spec 列了哪些 must-fail → 讓系統走到那點 → assert spec 規定的觀察值];❌[跑系統看會出什麼 → 結果＝spec/baseline]——後者做出來的不是測試,是現狀的指紋(§16.1).下游(測試、手冊、新實作)只從 spec 衍生;spec 沒寫具體的,回來補 spec(§15.4),不在測試或手冊端各自發明.

1. 讀 spec 直接衍生**可斷言的觀察值**:畫面文字原文、狀態、回應內容、落地檔案、DB 列——每條 predicate 對得到 spec 的某一句.
2. 遇歧義/缺漏問**具體那一點**,不是問整份.判別:❌[我列了 28 張 baseline 預期內容,你 review 一下];✅[spec 第 3 條的錯誤訊息沒指定 i18n 鍵,對應 X 還是 Y?].
3. 整份都不安＝還沒讀懂,回頭再讀,不是拿去問.
4. 讀 spec 之外仍須讀系統:spec 沒列的變體(資料型別、內容型別、入口)由系統列舉(§2.1 第 3 條)補進 spec 再衍生,不因 spec 沒提就略過.

### 15.3 spec 與現況不符時的處置:先確認是產品事實,再分類,不遷就

**觸發**:實作、測試、產圖、撰寫手冊或探測時,發現 spec 某句與程式碼或實機不一致.

**第一步:確認[不符]是產品事實,不是我的手段造成**——追鏈路到 `file:line`(§4 規則 6),並以不帶任何測試 hook 的環境重現(§4 規則 3).由截圖方式觸發重排、輪詢卡住過場、注入旗標、程式捲動等測試手段製造的現象不是產品問題,不進 spec,記於測試端之偏離紀錄.

**第二步:依下表分類處置**(四類標籤沿用〈已知落差〉之慣例,每條必帶其一):

| 情形 | 判定 | 處置 |
|---|---|---|
| spec 那句本是猜測或已過時(按鈕不存在、位置相反、其實不需遮罩、參數值不同),而現況本身合理 | spec 描述錯 | 直接改 spec 該句;〈已知落差〉記一條[原載『…』不成立:實測…]留痕 |
| 現況違反使用者可觀察之承諾:affordance 未兌現、同型元素互動不對稱、同一語意兩處行為不同、訊息與規則衝突 | **缺陷(待修)** | 〈已知落差〉記症狀、`file:line`、絕對日期、證據;對應測試步驟 pending(§16.1),baseline 不凍結壞畫面;spec 不改成壞行為 |
| 現況有依據(權限模型分軸、資料軸依表類別之規則)但 spec 沒寫 | **設計事實(記錄備查)** | 補進 spec/共用文件並附 `file:line`;不列待裁示,不呈報為漏洞 |
| 既有原則判不了,且業主不決定就無法往下 | **待裁示(業主決定)** | 呈報時附分類、各選項與代價;只放這一類;已裁示過者先查裁決紀錄,不重提 |
| 產品端已修好 | **已修復(附修復紀錄)** | 受影響案例同號改寫為新行為,舊觀察留〈已知落差〉附修復紀錄,不刪條目;下游測試、手冊同步 |

**聚合層之粒度**(常在寫 assertion 時撞到):**聚合層**＝實作把多種內部狀態併成同一句對外訊息(5 種失敗原因都顯示[操作失敗],3 種空狀態都畫同一張空圖).實作有[N 種具體狀態 → M 種顯示訊息]聚合時——spec 期望 N 種 → 聚合是缺陷,改實作;spec 期望 M 種 → 設計取捨,寫進 spec/文件;spec 沒說 → 補 spec(§15.4).三者都不是改 test 將就.

### 15.4 spec 缺漏或歧義:補 spec,不繞過 spec

- spec 未具體化的部分(錯誤訊息沒指定 i18n 鍵、空狀態畫什麼、邊界值、訊息時機)→ 回去補 spec:請使用者補,或我自擬候選並請確認**那一條**,確認後寫進 spec 才衍生.不繞過 spec 直接問[這情境要顯示什麼]——沒寫進 spec,其他 agent 看不到,下一輪又從頭問.
- 補進 spec 的每一句都要有依據(§15.1:程式碼或實機證成):具名資料須複驗於現行資料庫確實存在(資料庫重建後再複驗一次),數字皆實測,訊息寫原文.
- 補完標[原載 → 改為]與絕對日期(§17.2),讓下一個讀的人知道這句是後補的、依據是什麼.

### 15.5 撰寫、改寫、複審 spec

**凡新寫、改寫、擴充、複審 e2e 流程 spec(含 `E2E-NNN` 案例、〈已知落差〉、待裁示),或因 e2e 實作/標準圖產製/手冊產製/產品修復而回頭修 spec 之任務,必先調用 skill[role-writer-e2e-spec],整篇入 context 逐項比對後才動工.**

其他類型之 spec(API 規格、schema、流程/設計文件)沿 §17.1.不論類型,交付前逐條自檢:

1. 每句可被程式碼(`file:line`)或實機(探測紀錄)證成;
2. 每條規則可翻成可斷言之觀察值(§16.1 才有東西可翻);
3. 正向、負向(must-fail)、邊界同列,不只寫 happy path;
4. 訊息寫原文、樣式與時機;
5. 修改留痕(原載 → 改為,絕對日期);
6. 未具體化者明標[待補],不留空白讓下游猜;
7. 〈已知落差〉只收程式碼或實機證成之事實,每條帶 §15.3 四類標籤之一.

## 16. 測試

**觸發**:寫、改、審、拆、移除、重構任何測試;新增測試檔或盤查 test 目錄;產或重產 baseline;設定 `package.json` 之 test script、runner 或 port;跑測試;產製測試/樣本/示範數據.測試綠只是 §2 完成判準三件中的第①件(回歸),表覆蓋與使用者視角驗收仍須另做.

### 16.1 測試是規格的翻譯,不是現狀的指紋

assertion 必須是 spec 的可執行翻譯,不是[跟上次跑出來一樣]的指紋.[產 baseline → `buf.equals(baselineBuf)` → 通過]不是測試,是 regression detector——若 baseline 產出時系統已偏離 spec,偏離被凍結為真理,測試通過反而是 false negative.

1. **Reflex check**:寫每條 assertion 前自問[這條 predicate 對應 spec 哪一句?]對不上＝現狀指紋,刪或回 spec 找對應(§15.4);每條 assertion 註解標明對應之 spec 句.(LLM 偏好機械安全路徑如 `buf.equals`,此 check 就是擋這條.)
2. spec 含[畫面/顯示/轉跳/卡/出現/看到/看不到/進入]等觀察字眼 → 必須有對應之 UI 斷言,純後端 trigger 才能只驗 DB;spec[N 步後 → 觀察 M] → 測試步驟數 ≥ spec 動作數,被 helper 抹平的步驟明標[未走 UI].
3. 能驗端到端不變式(外部應用實際查到的結果)就驗它,不只驗中間資料存對.
4. 寫 assertion 時發現實作把 N 種狀態併成 M 種訊息 → 依 §15.3 聚合層規則處置,不改 test 將就.
5. 走到產品缺陷的步驟:依 §15.3 進〈已知落差〉,該步拋帶已知缺陷標記之錯誤讓 runner 標 pending;不用放行旗標讓壞畫面通過,不寫[baseline 凍結的是現況畫面].

### 16.2 語意斷言為主,pixel baseline 為補強層(雙向約束)

1. 語意斷言驗[特定資訊在/不在],pixel baseline 驗[整體畫面長相],抓的是不同類 regression;對有 stable visual state 的 case,baseline 是**必要補強層非 optional**.雙向約束:只有 pixel-equality 沒語意斷言不接受;modal/轉跳/UI 切換 case 只有 innerText/URL 不加 baseline 也不接受.
2. 補強層之 pixel 比對採**反鋸齒感知容差**,非 byte-exact——白話:不要逐 byte 比整張圖(`buf.equals`),改用[允許少量像素不同]的比對:pixelmatch(`includeAA:false` 自動忽略反鋸齒邊緣像素)+ `maxDiffPixels` 容差.why:SVG icon/字型邊緣之次像素 raster 跨 browser session 不決定性,byte-exact 會把[肉眼等同的反鋸齒 noise]當失敗 → 無止境 flake.**典型錯誤形狀**:icon 全面改用 SVG 後,原本的 byte-exact 比對炸出大量次像素 flake.容差只放補強層,主驗證仍語意斷言(不違反第 1 條);真 regression(icon 換/版面位移/顏色變)量級遠超 maxDiffPixels 仍被抓.同業界 Playwright `toHaveScreenshot`(maxDiffPixels/threshold).
3. 產 baseline 前,從 spec 自己列[每張預期看到什麼]清單;產實際 baseline → 比對 → 不吻合修系統/修 spec,不是改 baseline 將就;不把預期清單丟給使用者 review(§15.2).
4. 重產須先取得授權,且只產受影響者;無差別重產＝把 bug 凍結為真理.已審過的圖被重產等於換掉真理,即使內容看起來一樣.
5. 比對失敗之證據(capture/baseline/diff 三聯組)落專案根 `./testPending/`(§16.4),永不覆蓋、不自動刪——偶發 flake 的當次證據常在隔天才分析.

### 16.3 測試分層與檔名前綴:`e2e-` / `api-` / `unit-`

測試檔一律以前綴標明層級,**同一層 `test/` 目錄內以前綴分類,不開 `e2e/` `api/` `unit/` 子目錄**(子目錄只給資產與工具,見 §16.4):

| 前綴 | 層級 | 判準 | 典型 runner |
|---|---|---|---|
| `e2e-` | 端對端 | 真瀏覽器 + 真鍵盤滑鼠,act/assert 走 user-facing 路徑 | Playwright(mocha 逐檔或直跑) |
| `api-` | 介面 | 直打 HTTP/RPC 端點,不經 UI | mocha / jest |
| `unit-` | 單元 | 純函式,元件掛載(jsdom),不起 server | jest / mocha |

- 檔名 `e2e-xxx` 但 act 用 `page.evaluate` / `vm.method()` → 名實不符,**正名為 `api-` 或 `unit-`,或重寫成真 e2e**(§16.6 之 e2e 技能有 L1-L6 操作層級表).
- UI 無法自然觸發者(fail-closed、後端並發序列化)→ 下沉 `api-`/`unit-`,不在 e2e 硬造.
- 共用層與輔助工具(setup 模組、runner、產生器)不帶 `.test.` 中綴且放 `test/tools/`(§16.4),避免被 runner 當測試檔抓取.
- **runner 的納入規則用白名單不用黑名單**:如 jest `testMatch: ['**/unit-*.test.mjs','**/api-*.test.mjs']`,而非 `testMatch:['**/*.test.mjs']` + 逐檔 `testPathIgnorePatterns` 排除 e2e——後者新增 e2e 檔時忘了加排除,會被 jest 誤抓並在無瀏覽器環境炸開.
- 專案若無某層(如純前端函式庫無後端)則該前綴不出現,不需硬造.

### 16.4 目錄配置:什麼放哪,誰負責清

| 路徑 | 放什麼 | 規則 |
|---|---|---|
| `test/*.test.mjs` | 測試檔 | 一律 `.test.mjs`,依 §16.3 前綴分層;不開 `e2e/` `api/` `unit/` 子目錄 |
| `test/tools/*.mjs` | 輔助測試與配套工具:setup 共用層、逐檔隔離 runner、baseline 產生器 | 一律 `.mjs`,不帶 `.test.` 中綴;同一 helper 只定義一次,測試檔內第二份近似實作＝補丁堆積 |
| `test/pics/<flow>/` | e2e 標準圖(baseline) | 入版控;spec 或規範另有指定路徑者從其指定 |
| `test/<fixture>/` | fixture 資產(golden、expected、上傳用檔) | 入版控;不是中介資料,不放 `_tmp` |
| `test/_tmp/<案名>/` | 測試中之臨時/中繼檔(暫時 settings、fixture 副本、合成 log、資料庫快照) | gitignore;各測試自建專用資料夾,由該測試檔之 `after` 或 setup 之 cleanup 刪除,測完即刪,不殘留 |
| `./testPending/` | pixel 比對失敗之三聯組證據 | gitignore;ms 時間戳、永不覆蓋、**不自動刪**(人工清理) |
| `./tmp/` | AI 代理暫存區(§12.4) | **測試程式禁用**——隨時會被整個清除,測試途中被刪即假失敗.**典型錯誤形狀**:暫時 settings 與 golden 產生器寫在 `./tmp/`,隨一次清理佚失 |

### 16.5 執行:`package.json`、mocha 與 port

- **禁用 `.mocharc.json`**:會讓套件與專案之批次處理機制無法統一、無法自動化調整與擴充;不自行建立.
- **`package.json` 之 scripts 一律只有 `test`**(即 `npm test`):它放的是全部單元測試之指令,不是給 agent 塞多種指令的指令庫;不加 `test:e2e`、`test:unit`、`test:watch` 等變體——變更會讓批次處理機制無法統一.
- **要跑指定測試檔,自行編寫指令執行,不寫進 scripts**:如 `npx mocha test/unit-xxx.test.mjs --timeout 180000`;e2e 逐檔 `npx mocha test/e2e-<flow>.test.mjs --reporter list --timeout <依專案>`,有逐檔隔離 runner 者直跑 `node test/tools/<runner>.mjs`.超過 2 分鐘者 `run_in_background`(§12.8).
- **`test` 之值用 mocha 時,依有無 parallel 限定為下列字串**:
  - 有 parallel:`mocha --parallel \"test/*.test.mjs\" --timeout 180000`
  - 無 parallel:`mocha --no-parallel \"test/*.test.mjs\" --timeout 180000`
- **`npm test` 不含 e2e**:e2e 需真瀏覽器、獨立後端與逐檔隔離(多檔塞同一 mocha 進程會共用被前面測試改過狀態的後端),一律以自行編寫之指令逐檔跑.`test/` 內只有 `unit-`/`api-` 時,上列標準字串之 `test/*.test.mjs` 即為白名單;`test/` 內另有 `e2e-*.test.mjs` 時,glob 收窄為前綴白名單(`\"test/unit-*.test.mjs\" \"test/api-*.test.mjs\"`),其餘旗標不變(§16.3 白名單規則).
- **port**:測試要起伺服器者,port 一律 ≥ 8000,寫死於專案設定/映射表並與他專案錯開;多服務須有自動配發機制(自固定起點遞增偵測),**禁止隨機產生**——隨機常撞埠導致整批假失敗.測後釋放並回驗(§12.6);非自己啟動之伺服器先回報,不擅自殺.
- 跑完的判準不只是綠:`./tmp/` 清乾淨、`test/_tmp/` 無殘留、`netstat` 無殘留 server、`./testPending/` 無本輪新增(或已分析);子程序清理走 §12.9.

### 16.6 e2e 測試

**凡接觸 e2e 測試檔(檔名含 `e2e-`)的任務——寫/改/審/拆/移除/重構/完整度盤查/flake 排查/標準圖產製或重產——必先調用 skill[role-coder-for-test-e2e],整篇入 context 逐項比對後才動工.**

### 16.7 測試/樣本數據之連結、引用、外部參考必須真實可驗證,不可捏造

**觸發**:產製任何測試/樣本/示範數據,其欄位含 URL、外部參考文獻、報告編號/序號,或關聯到內部既有資料時.

**背景**:LLM 自由生成領域內容時天然會捏造[看似合理]的具體引用——假 URL,拿 `google.com/search?q=` 當參考連結,虛構報告編號(如自編 `SKB TR-XX-XX` 配自擬標題),同一個佔位 query 配不同假標題.這類數據交付業主/行政單位驗收時**一點即知為假 → 直接扣分甚至減價驗收**.這是[看起來像實作的虛構範例](§4 規則 2)在數據層的變體.

**鐵則**:
1. **連結須真實可連通**:不用 `google.com/search` 當參考連結;不捏造 URL 或報告編號.產出前 `curl -sI`(或 `-o /dev/null -w %{http_code}`)驗 HTTP 200.
2. **引用真實既有物**:內部關聯優先參照系統內**確實存在**的項目(真實節點/需求編號+名稱,由資料本身或關聯圖推導);外部參考只用**已驗證存在**之真實文獻——可沿用同專案其他模組已驗證之參考集(如既有真實模組之法規/報告 URL),不另憑空生.
3. **具體標識查不到就不給**:報告編號、序號、標題、DOI、法規條號等只能用真實查得者;查不到 → 欄位可空則留空,或改用泛稱,絕不自編.
4. **寧缺勿假**:欄位允許空值(如 `necessary:'n'`)時,無真實對應就留空,遠優於填假資料.
5. **委派子代理產內容必明文禁捏造**:子代理自由生成領域內容最易捏造連結/引用,派工 prompt 須明訂[只用可驗證之真實來源,禁 google query 當連結,禁自編報告編號],並要求可追溯來源;收回後逐欄查有無假 URL/假編號才接受.

**交付前自問**:[這個連結/引用,業主點下去或去查證,會成立嗎?]不確定＝不可交.

## 17. 文件與報表

**觸發**:撰寫流程文件,設計文件,呼叫鏈文件,數據分析報告時.

### 17.1 流程 / 設計 / 呼叫鏈文件

**凡撰寫流程文件,設計文件,呼叫鏈文件(trace 某函式,為 spec API event 產出 markdown 規格)的任務,必先調用 skill[role-writer-e2e-spec],整篇入 context 逐項比對後才動工.**

### 17.2 數據分析報告

報表必須使用實際日期(如 `03/27`),禁止[昨日/前天]等相對用詞——不能確定使用者何時看,相對日期會誤解.

### 17.3 回報誠實性

**觸發**:所有任務回報.

- 寫[全盤/完整/所有/全部檢查]之前,回報內必附 §2.1 第 3 條之表(或其統計:軸、格數、已測/刻意允許/已知不修/未填各幾格);沒有表不得用這些詞.只做了部分就寫[部分:做了 X,未做 Y].
- 回報之[完成]須同時滿足 §2 三件完成判準;缺項要明寫缺哪一項.
- 自身失敗之歸因依 §3[失敗歸因先找自己].
