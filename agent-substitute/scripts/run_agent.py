import sys\nimport json\nimport os\n\n# 模擬呼叫 sessions_spawn (在實際 OpenClaw 環境中，這通常是透過 tool call 達成，但在 script 中我們印出指令供 agent 執行)\n# 這裡我們讓 Agent 讀取這個 script 後，知道要執行什麼 tool call\n\ntask_content = sys.argv[1] if len(sys.argv) > 1 else \"等待指令...\"\n\nprint(f\"正在啟動 Substitute Agent 執行: {task_content}\")\nprint(\"請 Agent 執行 tool: sessions_spawn\")\nprint(json.dumps({\n    \"task\": f\"[Role: Substitute] {task_content}\\n\\n職責：根據主agent的要求，執行相應任務。\\n主要產出：依照任務執行狀況進行詳實回報，不論成功或失敗皆須回報。\",\n    \"model\": \"google-antigravity/gemini-3-pro-high\",\n    \"thinking\": \"low\",\n    \"cleanup\": \"keep\"\n}, ensure_ascii=False, indent=2))\n