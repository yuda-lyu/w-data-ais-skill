---
name: role-writer-flow-doc
description: |
  流程/設計/呼叫鏈文件撰寫規範技能：流程文件單一 scope 紀律（只寫本流程、嚴格不寫清單、看得到≠寫得進來、寫完前自問三題）、程式碼呼叫鏈文件之 trace 調研步驟與輸出格式（縮排即歸屬、每執行行標 [檔名:行號]、.then/.catch 攔截範圍呈現、3 位數步驟編號）。
  觸發條件：凡撰寫流程文件、設計文件、呼叫鏈文件（trace 某函式、為 spec API event 產出 markdown 規格）的任務——寫/改/審/重構——必先調用本技能，整篇入 context 逐項比對後才動工。
---

# 流程 / 設計 / 呼叫鏈文件撰寫規範

**觸發條件**:凡撰寫流程文件,設計文件,呼叫鏈文件的任務,本篇整篇入 context 逐項比對.

## 1. 撰寫流程 / 設計文件

一份流程文件只描述本流程,從進入到離開為止;不在本流程的事完全不提——連[不在此文件範圍]這種 scope 標註也不寫.使用者讀流程文件是執行性需求不是探索;資訊完整不是優點,是負擔.

**嚴格不寫清單**(任一條觸發該段刪除):跟某某流程的差異/對照表;連結到其他流程文件;`見 X.md` / `詳見` / `see also`;`為另一支獨立流程` / `不在此文件範圍`;[A 流程不做這個(B 流程才做)];設計取捨表內拿別的流程當對比;[對應/不像 XX 流程].

**看得到 ≠ 寫得進來**:UI 上看得到但行為屬另一流程的元件,只描述本流程內可觀察的事實(✅[password 欄位顯示為『重設密碼』按鈕,不接受就地輸入]),不展開點擊後的後續,不加 scope 註解,不引用其他文件.

**寫完前自問三題**:①刪掉這段,使用者完成本流程的能力會降低嗎?不會→刪.②這段在描述本流程不負責的事?是→刪.③這段提到其他流程文件/API/點擊後的後續?是→刪或改寫成本流程內可觀察事實.

## 2. 程式碼呼叫鏈文件(trace + 輸出格式)

**觸發**:使用者要求[寫流程文件/呼叫鏈文件/trace 這支函式/為 spec API event 產出 markdown 規格]時套用.核心準則:①鏈路完整(追到底不漏層);②可回溯(每執行行標 `[檔名:行號]`);③縮排即歸屬(縮排深度反映呼叫層級與 catch 攔截範圍).

**調研步驟**:①確認入口 → ②追蹤呼叫鏈到最底層,每層讀原始碼不憑記憶(全域規範 §4 規則 1+6)→ ③標每個 await/Promise(無 catch 的標[reject 時 throw 至 XXX]指明被哪個外層接住)→ ④標每個條件判斷 → ⑤記錄 file:line → ⑥過濾無關內容.

**輸出格式規則**:

- 縮排:每進一層呼叫 4 格;同函式內步驟同層級,不同函式內容不混層.
- 呼叫展開順序依實際執行:先展開被呼叫函式內部實作,再列 `.then` / `.catch`(內部執行完回傳後才觸發).
- await 回傳後繼續執行的部分回到呼叫者縮排層級,不留在被呼叫者縮排內.
- 函式內定義的 async function(如 `core()`):其內部步驟縮排在 core() 之下,`.catch` 與 core() 同層(才看得出攔截範圍).
- `.then` / `.catch` 獨立成行,內容換行縮排;本身要標 file:line.
- 條件判斷:描述[檢查什麼],各條件結果換行縮排;分支標籤行(如 `不一致:`)不標 file:line(非獨立執行行).
- 檔名行號放該行最末尾 `[檔名:行號]`.
- 流程步驟編號固定 3 位數(001,002…);行號範圍用 `-` 不用 `~`(避免 markdown 刪除線).
- 不列入:delay/sleep,loading 切換,console.log,純 log,不影響分支的變數賦值.
- 描述:清楚中文寫[做什麼]不寫 pseudo-code;函式名保留(如 `執行非同步流程 core()`);不壓縮一動作一行;reject 字串用單引號.

範例(縮排與歸屬):

```
呼叫 outerFunc.foo  [outerFile.mjs:xx]
    執行非同步流程 core()
        呼叫後端 funcA,無 catch,reject 時 throw 至 .catch  [xx.mjs:yy]
            [funcA 內部展開]
            比對密碼雜湊  [authService.mjs:263]
                不一致:
                    reject('incorrect user account or password')  [authService.mjs:264]
            回傳結果
        後端回傳成功,繼續前端處理  [xx.mjs:zz]    ← 回到核心流程層級
    .catch  [xx.mjs:ww]    ← 與 core() 同層
        處理錯誤
```

**輸出結構**:`# XXX 流程` → `## 觸發`(觸發點說明)→ `## 流程`(code block 巢狀列表)→ `## 補充表格`(視需要:檢查順序,錯誤處理層級,分流條件,i18n 訊息粒度等).
