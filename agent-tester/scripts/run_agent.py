import sys\nimport json\n\ntask_content = sys.argv[1] if len(sys.argv) > 1 else \"等待測試需求...\"\n\nprint(f\"正在啟動 Tester Agent 執行: {task_content}\")\nprint(\"請 Agent 執行 tool: sessions_spawn\")\nprint(json.dumps({\n    \"task\": f\"[Role: Tester] {task_content}\\n\\n職責：為 coder 產出的程式碼設計與撰寫測試案例。撰寫單元測試（Unit Test）與必要的整合測試（Integration Test）。驗證邏輯正確性、邊界條件與例外情境。模擬錯誤輸入、極端值與異常流程。回報可重現的 bug 與失敗測試結果。\\n主要產出：測試程式碼（如 Jest / Vitest / Mocha）、測試案例說明（測什麼、為什麼）、Bug 描述（重現步驟、預期結果、實際結果）。\",\n    \"model\": \"google-antigravity/gemini-3-pro-high\",\n    \"thinking\": \"low\",\n    \"cleanup\": \"keep\"\n}, ensure_ascii=False, indent=2))\n