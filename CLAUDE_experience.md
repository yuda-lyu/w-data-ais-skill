# 經驗案例庫(非常駐)

> **本檔性質**:自 `CLAUDE.md` 抽出的**個案細節**與**情境專用參考**,不隨每個 session 常駐載入.
> `CLAUDE.md` 只留原則與可直接執行的規則;規則的來由(哪個專案、哪天、賠了什麼)與只在特定情境才用得到的操作細節,放在本檔.

> **`CLAUDE.md` 刻意不指向本檔**:它會被帶到不同機器,必須單獨成立.因此規則所需的判準與[典型錯誤形狀]全部留在 `CLAUDE.md` 內文(去識別),本檔只補**是哪個案子、哪一天、賠了什麼**這一層,以及框架專屬的長篇細節.
> **何時讀**:①我懷疑某條規則是不是過度反應,想看它的來由 ②要把某節做成技能,需要完整素材 ③維護 `CLAUDE.md` 時要回頭確認某條規則的證據強度.
> **不必讀**:一般任務.`CLAUDE.md` 已自足.

> **維護**:某節反覆被用到,就獨立成技能;某案例的教訓已被更泛用的原則涵蓋,就刪案例留原則.

---

# 一、規則的來由:個案紀錄

每條格式為[規則 → 案例 → 教訓].案例是我當時真的賠掉的東西,不是假設.

## 1. 只沿投訴那一條線走,連三輪被退(對應 §2.1 背景、§2.1 第 1 條)

- **案例**:w-flow-vue,2026-08-26~27,三輪——刪除機制、齒輪閃爍、連入點死區.每輪都[讀了碼、實測、測試全綠],仍被使用者一點就破.
- **教訓**:三輪的共同形狀是**只沿投訴那一句話走一條線**,沒有回頭列同類的其他線.測試全綠只證明我測的那條線對,不證明類別完整.這些問題不需要 spec 就能知道(同類產品通用常規＋仔細列舉即可),所以不得把原因推給[沒有 spec / 環境 / 工具].

## 2. 自審的表必經他審,而且他審對象是表(對應 §9.1)

- **案例**:w-flow-vue 互動重構,2026-08-27.我自認完整的一張輸入空間矩陣,派給 Sol 複審後仍被找出 **30 格缺口,且全部屬實**.
- **教訓**:複審的價值在找出我漏掉的格,不在評價我已列的方案.所以派工 prompt 的第一順位永遠是[挑戰這張表的完整性],方案排最後.

## 3. affordance 的承諾被我抄成註解(對應 §2.1 第 6 條)

- **案例**:w-flow-vue 連入把手——有 hover 放大、cursor default,mousedown 卻在上層被丟棄.我讀到這個矛盾時,沒有把它列為缺陷,反而寫成註解[連入點不可作為出發點]替它背書.
- **教訓**:既有碼是現況不是規格.**把說不通的現況寫成註解,等於把缺陷升格成設計**,而且下一個讀到的人(或我自己)會直接繼承.

## 4. 順手 `cd` 造成連鎖權限確認(對應 §12.2)

- **案例**:2026-08 液化專案,為解壓 npm 套件用了 `cd ./tmp/wdc`,其後數條指令全部要求確認,使用者反問[為何現在這指令都會跳出來要我同意].
- **教訓**:`cd` 改掉的是**後續每一次呼叫**的 cwd,原本正確的相對路徑全部指錯,為了修正又得再 `cd` 回去——一次偷懶換來一整串打斷.

## 5. heredoc 寫暫存腳本,同樣觸發連鎖確認(對應 §12.2)

- **案例**:同專案以 `cat > ./tmp/zz_chkDone.mjs << 'EOF'` 寫暫存腳本被要求確認,使用者反問[為何又出現會要我確認指令].
- **教訓**:[只是暫存腳本]不是放寬的理由.shell 寫檔既觸發確認,又不顯示 diff,錯了還是靜默寫出壞內容.

## 6. `grep -r` 在資料型專案連續逾時兩次(對應 §12.3)

- **案例**:液化專案同一輪對話內,因 `grep -r` 硬掃全樹連續吃滿 120s timeout 兩次;當時我以為加了 `--include="*.mjs"` 就安全.
- **教訓**:`--include` 只決定要不要讀檔案內容,grep 仍須走遍每個目錄、對每個檔案 stat.**瓶頸是檔案數量不是內容大小**,所以 `--include` 完全救不了.

## 7. 全量重產在 session 更替時中斷(對應 §12.8 鐵則三)

- **案例**:液化專案 2026-07-31,全量重產於 session UUID 更替時被一併終止,**992 項只完成 239 項**.
- **教訓**:`run_in_background` 的行程掛在 session 之下.判準是[會不會跨 session],不是[跑多久].

## 8. Chrome 孤兒程序逐次疊加(對應 §12.9,細節見第二部分 C)

- **案例**:2026-07 液化專案實測＋讀 playwright 原始碼確認.node 被 TerminateProcess 時,playwright 掛在 exit handler 的 `taskkill /T /F` 全不執行,chrome 整樹孤兒化;服務自動重啟則逐次疊加.
- **教訓**:上游沒有要修(同類 issue 均 no-repro/推上游收場),`--remote-debugging-pipe` 斷線自關是盡力而為非保證,**不能當防線**,必須自建回收.

## 9. 固定尺寸拍腦袋 → 右側大片留白(對應 §10.6 第 5 條)

- **案例**:frliq 佈局下拉,最長內容 `'2:2'` 實寬僅 **16px**,加上內部結構(padding/邊框/icon/間隙)自然寬約 **50px**,卻拍腦袋給 **74px**,右側留白突兀.
- **教訓**:[看起來夠]這個判準本身就是未經設計.量最長項實寬＋逐項加總結構,是唯一能講出理由的給法.

## 10. 窄選單照搬寬列表的高亮語言(對應 §10.6 第 6 條)

- **案例**:frliq 佈局下拉(**54px** 窄選單)照搬地震列表的左緣 bar 高亮,選項 padding **13px** 又與 trigger **9px** 不一致,雙重破壞文字對齊參考線.
- **教訓**:高亮方式會隨容器寬度改變性質——在寬列表無害的左緣 bar,在窄選單會讓文字看起來右偏.**照搬的是像素,不是設計語言**.

## 11. 按鈕群拆搬後未量測即交付(對應 §10.6 第 1、2 條)

- **案例**:frliq 按鈕群拆搬後,接排間隙 **17px** vs 原生 **5px**,未量測即交付;佈局下拉未盤點父層深色玻璃色票即隨手配色.
- **教訓**:任務被框成[功能搬移/重構]時,我會自動把[讀 API → 量數字 → 盤設計語言]整套跳過.觸發要以**對象**判定,不以任務類型判定.

## 12. 永遠朝下的 ▾(對應 §10.6 第 4 條)

- **案例**:frliq 佈局下拉配了 0.6rem、永遠朝下、不隨開闔變化的 ▾.
- **教訓**:靜止不動的指示符號會被使用者當成壞掉,**半吊子指示比沒有更糟**.狀態接不出來就不要放.

## 13. 線上頁首中文 logo 破圖(對應 §11.5,細節見第二部分 A)

- **案例**:frliq 線上頁首中文 logo 破圖.ASCII 資產(css/js)恰好正常,只有中文檔名的圖掛掉,極易被誤判為[圖檔沒上傳].
- **教訓**:`request.path` 保留 percent-encoding,直接 `path.join` 找不到檔案時**靜默落入 SPA fallback,回 200 + text/html**——回 200 讓人以為服務正常,實際回的是 HTML.

## 14. byte-exact baseline 炸出無止境 flake(對應 §15.2 第 2 點)

- **案例**:icon 全面改用 @mdi/js SVG 後,原本的 byte-exact baseline 比對炸出大量次像素 flake.
- **教訓**:SVG icon 與字型邊緣的次像素 raster **跨 browser session 不決定性**,byte-exact 會把肉眼等同的反鋸齒 noise 當成失敗.

---

# 二、情境專用參考(命中才讀)

以下三節只在踏進該情境時才有用,`CLAUDE.md` 留有觸發條件與一句話警語.

## A. HTTP 靜態檔案服務:request 路徑是 URL-encoded,解碼與穿越防護必成對

**觸發**:以 Hapi/Express 等自建靜態檔案服務(SPA dist 之 `{any*}` 路由),或以 request 路徑組裝檔案系統路徑時.

- `request.path`/`url.pathname` **保留 percent-encoding**(§11.2 同族:URL 空間字串≠檔案路徑).直接 `path.join` 去找檔案,非 ASCII 檔名(中文圖檔)必找不到而**靜默落入 SPA fallback → 回 200+text/html → 瀏覽器破圖**;ASCII 資產(css/js)恰好正常,極易誤判為[圖檔沒上傳].案例見第一部分第 13 條.
- 修正三要件缺一不可:①`decodeURIComponent` 包 try/catch(非法 % 序列拋 URIError;框架層可能先擋 400,try/catch 屬縱深防禦);②**解碼後必做路徑穿越防護**——未解碼時 `%2e%2e` 恰好擋住 `..`,加了解碼等於同時打開穿越風險,兩者必須成對;③落點檢查用 `path.resolve` 後絕對路徑且尾端補 `path.sep` 再 `startsWith`(只比 `startsWith(fdAbs)` 會放行 `dist-secret` 這類同前綴兄弟目錄).
- Hapi/Inert 之 `h.file` 預設 `confine`:檔案落在 `relativeTo`(預設 cwd)之外一律 **403 且無說明**;Windows 磁碟機代號大小寫不同即視為不同路徑(`d:\...` vs `D:\...` → 全 403).服務目錄一律用 cwd 相對路徑交 `path.resolve` 展開,勿寫死絕對路徑.
- 驗收不可只看 200:**SPA fallback 也回 200**,須對[非 ASCII 檔名請求]斷言 Content-Type(如 image/png 非 text/html);另測穿越(`/%2e%2e%2f...` 不得取得服務目錄外檔案)與非法 percent(`/%` 不得 500).

## B. `gcloud compute scp` 傳檔

**觸發**:以 gcloud 對 GCE 執行個體傳檔時.

遠端目的一律用絕對路徑 `/home/<user>/...`,絕不用 `~`(底層 pscp 不展開 `~`,會 `unable to open`;但 `ssh --command` 內的 `~` 正常展開,別搞混).中文來源路徑,Unicode 檔名,磁碟機代號實測全支援,不需 ASCII 中轉.不要用 `cat | ssh "cat > file"` 傳二進位(Windows 下 stdin 串流不可靠),傳檔就用 scp.

## C. Windows 背景 Chrome(playwright/puppeteer)程序回收

**觸發**:任何專案以 node 調用 chrome/chromium 背景繪圖或截圖(playwright、puppeteer、w-html2png 系),涉及常駐服務、排程重啟、或需清理 chrome 殘留時.

**孤兒成因**(實測＋讀 playwright 原始碼確認,案例見第一部分第 8 條):Windows 上 playwright 以 non-detached spawn 瀏覽器且不用 Job Object,唯一殺瀏覽器的 `taskkill /T /F` 只掛在 node 的 exit/signal handler 裡——node 被 **TerminateProcess**(`taskkill /F`、崩潰、OOM)時 handler 全不執行,chrome 整樹孤兒化;服務自動重啟則逐次疊加.`--remote-debugging-pipe` 斷線自關是**盡力而為非保證**(輕載機器會自關,重載/卡頓機器實測不會),不能當防線.playwright 官方對此無解也不會修,須自建回收.

**有效解法(實測驗證,三層)**:

1. **常駐服務啟動自掃**:埠綁定成功後(證明無其他實例服役),以 wmic(無 wmic 之系統改用 `Get-CimInstance Win32_Process`,§12.5)查 chrome.exe 之 ExecutablePath(**限定自家瀏覽器路徑**避免誤殺使用者瀏覽器)+ **父行程已死** → 對主程序樹狀殺.覆蓋「崩潰+自動重啟」情境,監護者無關(pm2/自寫 runner 皆有效).
2. **定期看門狗掃「已停止(stopped)」chrome**:卡死/已暫停的 chrome 用一般枚舉或年齡判準**看不出來**,必須用 `w-process-status` 之 `WProcessStatus('chrome.exe')` 查 `status==='stopped'` 才找得到,再交叉自家路徑後樹狀殺——此判準天然安全:服役中瀏覽器為 running 不會誤殺;卡死者(不論現任或遺留)殺後 alive 常駐模式下次請求自動重啟即自癒.
3. **監護者正常關閉走樹殺**:pm2 內建 treekill;自寫 runner 的 shutdown 須用 `wsemi` 之 `execProcessKillPid`(內部 tree-kill,Windows= `taskkill /pid X /T /F`),不可用 `child.kill()`(Windows 等同 TerminateProcess 且只殺 node 本身).

**鐵則**:①殺 chrome 必殺 root 帶 `/T`——逐個殺 renderer 無效,孤兒主程序會立即補生(打地鼠);②判準用「歸屬(父死)+健康(stopped)」,**絕不用年齡**——alive 常駐瀏覽器持續有請求時合法活數天,年齡判準會週期性處決健康現任;③`HandleCount=0` 之終止中卡死殭屍任何工具都殺不掉(回報「查無執行中實例」),屬 OS 層狀態,殺掉 root 後不增生即可接受,重開機歸零.
